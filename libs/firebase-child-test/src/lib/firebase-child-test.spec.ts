import { firebaseChildTest } from './firebase-child-test';

describe('firebaseChildTest', () => {
  it('should work', () => {
    expect(firebaseChildTest()).toEqual('firebase-child-test');
  });
});
